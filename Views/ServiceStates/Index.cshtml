@model IEnumerable<bikevision.Models.ServiceState>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_ManagementLayout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.state)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.state)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idServiceState }) |
            @Html.ActionLink("Details", "Details", new { id=item.idServiceState }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idServiceState })
        </td>
    </tr>
}

</table>
