@model IEnumerable<bikevision.Models.FeatureValueOfItem>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_HeadingLayout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Feature.feature1)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.FeatureValue.featureValue1)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Item.name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Feature.feature1)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.FeatureValue.featureValue1)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Item.name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
        </td>
    </tr>
}

</table>
