@model bikevision.Models.FeatureValueOfItem

@{
    ViewBag.Title = "Edit";
    Layout = "~/Views/Shared/_ManagementLayout.cshtml";
}

<h2>Edit</h2>

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    
    <div class="form-horizontal">
        <h4>FeatureValueOfItem</h4>
        <hr />
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        @Html.HiddenFor(model => model.Feature_idFeature1)

        @Html.HiddenFor(model => model.Item_idItem1)

        @Html.HiddenFor(model => model.FeatureValue_idFeatureValue)

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Save" class="btn btn-default" />
            </div>
        </div>
    </div>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>
