@model IEnumerable<bikevision.Models.ObjectType>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.type)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.type)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idObjectType }) |
            @Html.ActionLink("Details", "Details", new { id=item.idObjectType }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idObjectType })
        </td>
    </tr>
}

</table>
