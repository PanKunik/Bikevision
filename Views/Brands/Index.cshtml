@model IEnumerable<bikevision.Models.Brand>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_HeadingLayout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.brand1)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.brand1)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idBrand }) |
            @Html.ActionLink("Details", "Details", new { id=item.idBrand }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idBrand })
        </td>
    </tr>
}

</table>
