@model IEnumerable<bikevision.Models.PermanentDiscount>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_ManagementLayout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.discount)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.treshold)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.discount)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.treshold)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idPermanentDiscount }) |
            @Html.ActionLink("Details", "Details", new { id=item.idPermanentDiscount }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idPermanentDiscount })
        </td>
    </tr>
}

</table>
