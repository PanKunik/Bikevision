@model IEnumerable<bikevision.Models.FeatureValue>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_HeadingLayout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.featureValue1)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.featureValue1)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idFeatureValue }) |
            @Html.ActionLink("Details", "Details", new { id=item.idFeatureValue }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idFeatureValue })
        </td>
    </tr>
}

</table>
