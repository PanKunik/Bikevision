@model IEnumerable<bikevision.Models.Locality>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.locality1)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.locality1)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idLocality }) |
            @Html.ActionLink("Details", "Details", new { id=item.idLocality }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idLocality })
        </td>
    </tr>
}

</table>
