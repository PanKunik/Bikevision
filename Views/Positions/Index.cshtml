@model IEnumerable<bikevision.Models.Stanowisko>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.nazwa)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.nazwa)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idStanowisko }) |
            @Html.ActionLink("Details", "Details", new { id=item.idStanowisko }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idStanowisko })
        </td>
    </tr>
}

</table>
