@model IEnumerable<bikevision.Models.TypSprzedazy>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.typ)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.typ)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idTypSprzedazy }) |
            @Html.ActionLink("Details", "Details", new { id=item.idTypSprzedazy }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idTypSprzedazy })
        </td>
    </tr>
}

</table>
